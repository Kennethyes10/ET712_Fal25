/*
Homework 1: control flow and loops
Student’s name: Kenneth Soriano
*/
console.log("\n------ Program 1: conditional statement -----")
//ask the user to enter something
let userInput = prompt("Enter something")
// check that the user clicked"cancel"
if (userInput === null) {
    console.log("null and avoid")
}
// check that the user input have an empty string(clicked OK without typing nothing)
else if (userInput.trim() === "") {
    console.log(" the answer was blank!");
}
// check if the input is a Number
 else {
    let num = Number(userInput);
    
    // if its nit a valid number
    if (isNaN(num) ) {
        console.log("is not a number");
    }
    // if is a positive Number
    else if (num> 0) {
        console.log("think is positive!");
    }
    
    // if is a negative Number
    else if (num< 0) {
    console.log("never negative!");
}
// iys zero
else {
    console.log("Yin and Yang")
}
}

console.log("\n------ Program 2:  for loop and nested conditional statement-----")
// empty array
let numbers = [];
//ask the user to enter 10 numbers
for (let i = 0; i < 10; i ++) {
    let num = prompt("enter a number");
    num = parseInt(num);
    if (isNan(num) 
    }
